name: Test Workflow

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "✅ Workflow parsed correctly and is running on GitHub Actions"